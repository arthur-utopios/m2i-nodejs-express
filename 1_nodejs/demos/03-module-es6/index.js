import {area, perimeter} from './square.js';

console.log(area(5));

console.log(perimeter(6));

// rajouter "type": "module" dans le package.json pour utiliser la syntaxe de module de ES6
// Ou utiliser l'extension fichier.mjs pour module javascript